# ArgusV2 Formal Verification Audit Report

**Generated**: 2026-03-24 04:13 UTC
**Reasoning Provider**: Anthropic claude-sonnet-4-6  
**Verification Engine**: Lean 4 / Dafny

---

## Executive Summary

1 vulnerability was automatically repaired and re-verified.

**Risk Level**: ATTENTION REQUIRED — Auto-repaired or unverified files require human review before merging.

---

## Verdict Summary

| File | Verdict | Engine | Finding |
|:---|:---|:---|:---|
| `demo_target/drift_withdrawal.py` | **FIXED** | lean | Repaired and verified |

---

## Detailed Analysis

### `demo_target/drift_withdrawal.py`

**Verdict**: 🔧 FIXED  
**Engine**: `lean`  
**Finding**: Repaired and verified

> **Auto-Repaired**: Argus detected a vulnerability and generated a verified repair.
> Review the repaired code before merging.

#### Security Obligations

| ID | Property | Severity | Status |
|:---|:---|:---|:---|
| `withdraw:non_negative_result` | `withdraw(...) >= 0` | CRITICAL | PASS |

#### Assumed Inputs

| Property | Source | Evidence ID |
|:---|:---|:---|
| `balance >= 0` | policy: AccountPolicy.v1 | `EID-BAL-01` |
| `amount >= 0` | api_schema: WithdrawalRequest.schema.v2 | `EID-AMT-01` |

#### Repair: Before / After

**Original (vulnerable)**:
```python
"""
Demo Scenario 3 — Subtle Drift (expected verdict: FIXED)

This function looks safe at first glance but has a subtle overflow risk.
The fee calculation can push the result below zero when both amount and fee
are deducted from a small balance.

This is the "drift" scenario: code that passes manual review but fails
formal proof on the original implementation, then re-verifies after repair.

Narrative: "This function looks safe. But the fee calculation means balance
can go negative: withdraw(10, 9) → 10 - 9 - 0 = 1 (ok), but
withdraw(10, 10) → 10 - 10 - 1 = -1 (negative balance!). Argus catches
what code review misses and repairs it before merge."
"""


def withdraw(balance: int, amount: int) -> int:
    """Withdraw funds with a 10% processing fee.

    BUG: fee is computed on amount, but both are subtracted from balance.
    When amount is at or near balance, the fee deduction pushes result negative.
    Example: withdraw(10, 10) == -1  <- unauthorized fund creation
    """
    fee = amount // 10
    return balance - amount - fee
```

**Repaired (formally verified)**:
```python
def withdraw(balance: int, amount: int) -> int:
    fee = amount // 10
    total = amount + fee
    if total > balance:
        return 0
    return balance - total
```

---

## Risk Assessment

**Overall Risk Level**: ATTENTION REQUIRED

| Verdict | Count | Meaning |
|:---|:---|:---|
| VERIFIED | 0 | Formally proven safe — all obligations pass |
| FIXED | 1 | Vulnerability found and automatically repaired |
| VULNERABLE | 0 | Obligation failed — merge should be blocked |
| UNVERIFIED | 0 | Unsupported constructs — manual review required |
| ERROR | 0 | Tooling/runtime error — re-run or escalate |

---

## Recommendations

1. **Review auto-repaired code** — Argus generated and verified a fix, but human review is good practice.
2. Confirm the repaired logic matches business intent before merging.

---

## Audit Metadata

- **Tool**: ArgusV2 v2.1.0
- **Provider**: Anthropic claude-sonnet-4-6
- **Model**: claude-sonnet-4-6
- **Timestamp**: 2026-03-24 04:13 UTC
- **Files Audited**: 1

_Generated by ArgusV2 — Claude proposes. Lean disposes. Argus enforces._