# Backup Artifact Provenance

This directory stores fallback demo artifacts generated from real Argus runs.

For each scenario include:
- commit hash
- exact command
- UTC timestamp
- observed verdict
- generated artifact list

## Current status
Provider-backed regeneration is pending in target environment where `ANTHROPIC_API_KEY` is configured.
