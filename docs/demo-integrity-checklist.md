# Demo Integrity Checklist

## Production-path attestation
- Demo commands use `python -m src.adapters.cli` (same CLI path as CI)
- No demo-only execution flags in source
- `grep -RIn "demo_mode\|if demo\|DEMO" src` -> no matches (executed 2026-03-16 UTC)

## Fixture transparency
- Demo fixtures live in `demo_target/` and are explicitly documented
- Seeded benchmark corpus remains in `benchmarks/seeded/` for automated gate validation

## No hidden manual intervention
- Required setup is standard dependency install + provider key
- Verdicts and artifacts are generated by regular pipeline commands

## Backup artifact provenance
- Directory: `demo_target/backup_artifacts/`
- For each backup set include commit hash, command, timestamp, observed verdict in `PROVENANCE.md`

## Repro commands
```bash
git checkout <commit>
python3 -m pip install -r requirements.txt
PYTHONPATH=. python3 -m src.adapters.cli --file demo_target/safe_transfer.py --allow-local-verify --provider anthropic
PYTHONPATH=. python3 -m src.adapters.cli --file demo_target/vulnerable_transfer.py --allow-local-verify --provider anthropic
PYTHONPATH=. python3 -m src.adapters.cli --file demo_target/drift_withdrawal.py --allow-local-verify --provider anthropic
```

## Judge confidence statement
The demo uses the same product code paths, verdict logic, and artifact contracts as normal operation; no demo-only bypass path is present.
